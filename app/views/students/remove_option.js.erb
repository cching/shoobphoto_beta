$(".<%= @op.id %>_<%= @option.id %>").hide();

<% unless @op.options.any? %>
$("#myModal").modal('hide');
<% end %>

$("#cart_total").html("<%= number_to_currency(@cart.price, :show_zero => false) %>");
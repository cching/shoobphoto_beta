<% if !defined?(background) || background.nil? || background.to_i == 0 %>
	<% if type_class == "fivebyseven" %>
				<table>
					<tr>
						<td class="leftimage">

							<%= image_tag(image, class: "img-formatted") %> 
						</td>
						<td class="rightimage">
							<%= image_tag(image, class: "img-formatted") %> 
						</td>
					</tr>
				</table>
		

	<% elsif type_class == "wallets" %> 
			<table>
					<tr>
						<td class="leftimage">
							<%= image_tag(image, class: "img-formatted") %> 
						</td>
						<td class="leftimage">
							<%= image_tag(image, class: "img-formatted") %> 
						</td>
						<td class="leftimage">
							<%= image_tag(image, class: "img-formatted") %> 
						</td>
						<td class="leftimage">
							<%= image_tag(image, class: "img-formatted") %> 
						</td>
					</tr>
					<tr>
						<td class="leftimage">
							<%= image_tag(image, class: "img-formatted") %> 
						</td>
						<td class="leftimage">
							<%= image_tag(image, class: "img-formatted") %> 
						</td>
						<td class="leftimage">
							<%= image_tag(image, class: "img-formatted") %> 
						</td>
						<td class="leftimage">
							<%= image_tag(image, class: "img-formatted") %> 
						</td>
					</tr>
			</table>
	<% else %>
		<%= image_tag(image, class: "img-formatted") %>
	<% end %>

<% else %>

	<% if type_class == "fivebyseven" %>
				<table>
					<tr>
						<td class="leftimage">

							<div class="image_container">
								<%= image_tag(Background.find(background).image.url, class: "image_responsive") %>
								<%= image_tag(image, class: "image_responsive_watermark") %>
							</div>
						</td>
						<td class="rightimage">
							<div class="image_container">
								<%= image_tag(Background.find(background).image.url, class: "image_responsive") %>
								<%= image_tag(image, class: "image_responsive_watermark") %>
							</div>
						</td>
					</tr>
				</table>
		

	<% elsif type_class == "wallets" %> 
			<table>
					<tr>
						<td class="leftimage">
							<div class="image_container">
								<%= image_tag(Background.find(background).image.url, class: "image_responsive") %>
								<%= image_tag(image, class: "image_responsive_watermark") %>
							</div>
						</td>
						<td class="leftimage">
							<div class="image_container">
								<%= image_tag(Background.find(background).image.url, class: "image_responsive") %>
								<%= image_tag(image, class: "image_responsive_watermark") %>
							</div>
						</td>
						<td class="leftimage">
							<div class="image_container">
								<%= image_tag(Background.find(background).image.url, class: "image_responsive") %>
								<%= image_tag(image, class: "image_responsive_watermark") %>
							</div>
						</td>
						<td class="leftimage">
							<div class="image_container">
								<%= image_tag(Background.find(background).image.url, class: "image_responsive") %>
								<%= image_tag(image, class: "image_responsive_watermark") %>
							</div>
						</td>
					</tr>
					<tr>
						<td class="leftimage">
							<div class="image_container">
								<%= image_tag(Background.find(background).image.url, class: "image_responsive") %>
								<%= image_tag(image, class: "image_responsive_watermark") %>
							</div>
						</td>
						<td class="leftimage">
							<div class="image_container">
								<%= image_tag(Background.find(background).image.url, class: "image_responsive") %>
								<%= image_tag(image, class: "image_responsive_watermark") %>
							</div>
						</td>
						<td class="leftimage">
							<div class="image_container">
								<%= image_tag(Background.find(background).image.url, class: "image_responsive") %>
								<%= image_tag(image, class: "image_responsive_watermark") %>
							</div>
						</td>
						<td class="leftimage">
							<div class="image_container">
								<%= image_tag(Background.find(background).image.url, class: "image_responsive") %>
								<%= image_tag(image, class: "image_responsive_watermark") %>
							</div>
						</td>
					</tr>
			</table>
	<% else %>
		<div class="image_container">
								<%= image_tag(Background.find(background).image.url, class: "image_responsive") %>
								<%= image_tag(image, class: "image_responsive_watermark") %>
							</div>
	<% end %>
<% end %>


<style>
.image_container{position:relative; max-width: 100%;}
.image_responsive_watermark{position:absolute; top:0; left:0;max-width: 100%;}
.image_responsive{max-width: 100%;}

</style> 

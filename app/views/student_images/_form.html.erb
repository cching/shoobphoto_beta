<%= form_for(@student_image) do |f| %>
  <% if @student_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student_image.errors.count, "error") %> prohibited this student_image from being saved:</h2>

      <ul>
      <% @student_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :package_id %><br>
    <%= f.number_field :package_id %>
  </div>
  <div class="field">
    <%= f.label :student_id %><br>
    <%= f.number_field :student_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<div class="modal-dialog" id="lesson_resources">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
        <h4 class="modal-title" id="myModalLabel">Select Template</h4>
      </div>
      <div class="modal-body centered">
<div id="table">
  <div class="row">
  <div class="col-sm-3"></div>
  <div class="col-sm-6">
    <% if @types.nil? %>
    <h3>This student is missing a correct image. Please upload another.</h3>
    <% else %>
        <% @school.types.each do |type| %>
          <% if !type.pdf.nil? && type.pdf.template.columns.include?(Column.find(20)) %>
            <%= link_to type.name, export_custom_input_path(params[:id], type.id, @package.id), :target => "_blank", class: "btn btn-custom btn-centered", id: "type_#{type.id}", remote: true %>
          <% else %>
            <%= link_to type.name, export_form_path(params[:id], type.id, @package.id), :target => "_blank", class: "btn btn-custom btn-centered btn_no_custom", id: "submit" %>
            <% end %><br/>
        <% end %>
    <% end %>
      </div>
  <div class="col-sm-3"></div>
</div>
</div>

      <div class="form-actions">
      <div class="modal-footer">
        <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
      </div>   
      </div>   

    </div>
  </div>

<script>
$(".btn_no_custom").on("click", function() {
  $('#myModal').modal('hide');

});
</script>
<img src='<%= @url %>', class="img-rounded img-package-custom"><br /><br />
	<%= link_to "Remove", order_delete_package_path(:cart_id => @cart.cart_id, :package => @package.id, :index => @index, :url => @url, :student => @student.id), class: "btn-custom5", remote: true %> 

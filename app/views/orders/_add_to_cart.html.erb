<img src='<%= @url %>', class="img-rounded img-package"><br /><br />
	<%= link_to "Select", order_create_package_path(:cart_id => @cart.cart_id, :package => @package.id, :index => @index, :url => @url, :student => @student.id), class: "btn btn-custom", remote: true %> 


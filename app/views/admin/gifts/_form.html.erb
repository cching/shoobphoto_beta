<%= simple_form_for([:admin, @gift]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
  	<div class="field">
    <%= f.input :name %>
    <%= f.input :description, as: :string %> 

    <%= f.input :price, label: "Price (in $)" %>


    <%= f.input :image, as: :file %>

  </div>

  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
 
<h4 align="center"> CSV Import Instructions </h4>
<div align="center">

Lay out the CSV with the first row headers:<br />

<%= image_tag("package_csv.jpg", class: "img-rounded") %><br />
</div>
Where:
<ul>
<li>student_id is the ID of the student</li>
<li>url is the file name</li>
<li>grade is the grade/year taken</li>
<li>folder is the S3 location, such as /images/folder/url</li>
<li>dob is the student's date of birth, in the format year-month-day</li>

</ul>
<br />

<div align="left">
<%= form_tag admin_import_packages_path, multipart: true do %>
  <%= collection_select(:school, :id, @package.schools.order(:name), :id, :name, {:prompt => false}, {:class => "form-control"}) %><br />
  <%= file_field_tag :file %>
  <%= submit_tag "Import", class: "btn btn-primary" %>
<% end %>
</div>
<div class="row">
    <%= render 'categories' %>
    <div class="col-sm-9">
      <div id="itemlist">
      <%= render 'items', :locals => {:items => @items, :cart => @cart} %>
    </div>
    </div>
</div>
<br/>
